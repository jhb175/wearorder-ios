# 衣序 WearOrder 上架前 QA 清单

最后更新：2026-04-24

## 目标

这份清单用于 TestFlight 和 App Store 提交前的最后一轮体验验证。所有 P0/P1 问题都必须修复后再提交审核；P2 问题需要确认不会影响核心闭环、隐私披露、权限说明和数据安全。

## 自动检查

- 运行 `xcodebuild test`。
- 运行 `./scripts/run_local_smoke_test.sh`。
- 真实公开联系方式准备好后，运行 `./scripts/configure_release_contacts.sh` 一次性同步 App 内配置和上架资料。
- 真实隐私政策 URL、支持 URL 和支持邮箱配置完成后，运行 `./scripts/audit_app_store_readiness.sh --strict`。

## 手动验证顺序

| 顺序 | 路径 | 入口 | 建议设备 | 通过标准 |
| --- | --- | --- | --- | --- |
| 1 | 首次启动 | 首次安装后打开 App | 模拟器即可 | 不自动写入演示数据；Release 首次引导只在空衣橱出现；可添加衣物或稍后再说，不露出演示数据入口。 |
| 2 | 空衣橱引导 | 首页、衣橱、OOTD、计划页 | 模拟器即可 | 所有空状态都有明确下一步；推荐、OOTD、计划入口能说明缺少的衣物条件。 |
| 3 | 天气预报 | 首页天气卡片、WeatherKit 详情页和推荐入口 | 真机 | 没有手动天气/温度控件；授权定位后按 Apple Weather 真实预报推荐；定位拒绝时可选择城市天气；网络不可用或 WeatherKit 未开启时有清楚反馈。 |
| 4 | 添加与编辑衣物 | 首页快捷操作或衣橱页加号 | 真机 | 相册、相机权限文案正确；图片保存前压缩；详情、编辑、导出和删除影响提示正常。 |
| 5 | 创建 OOTD | OOTD 页、首页快捷操作或推荐结果 | 模拟器即可 | 上装和下装/裙装可保存搭配；今日搭配能回到首页；包袋和配饰在详情页分别显示。 |
| 6 | 创建计划与提醒 | 计划页、首页快捷操作或 OOTD 详情 | 真机 | 必须绑定 OOTD；未来提醒能创建本地通知；过期、缺失时间、权限拒绝都有反馈并关闭无效开关。 |
| 7 | 备份恢复 | 首页数据安全或设置页 | 真机 | 有数据时可导出 JSON；恢复时合并数据；恢复后同步可用提醒并显示同步数量。 |
| 8 | 隐私与支持 | 设置页隐私与支持 | 模拟器即可 | 本地优先、不追踪、不上传衣橱数据说明清楚；天气定位和城市天气披露准确；真实 URL 和邮箱填入后可打开。 |

## 缺陷分级

| 级别 | 阻塞策略 | 示例 |
| --- | --- | --- |
| P0 | 立即停止提交 | 崩溃、数据丢失、无法启动、隐私披露与实际行为冲突。 |
| P1 | 修复后再提交 | 核心路径按钮无效、提醒状态错误、备份无法恢复、真实数据被示例数据污染。 |
| P2 | 评估后决定 | 文案不清、边缘空状态不完整、统计提示不够准确。 |
| P3 | 可进入后续迭代 | 视觉微调、动效细节、非核心效率优化。 |

## 最终放行条件

- `WardrobeReleaseQAChecklist.orderedFlows` 的 8 条路径均已人工通过。
- XCTest、冒烟脚本和 Release 构建全部通过。
- `./scripts/audit_app_store_readiness.sh --strict` 无阻塞项。
- `APP_STORE_SCREENSHOTS.md` 中 5 张截图已按最终版本重新拍摄。
